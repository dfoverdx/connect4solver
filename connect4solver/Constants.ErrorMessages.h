#pragma once
namespace connect4solver {
    constexpr auto TODO_VERBOSE = "TODO: create messages/warnings map (not unordered_map) in order to print messages in SearchTree.IO.cpp rather than directly from thread";
}